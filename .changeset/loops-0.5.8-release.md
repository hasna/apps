---
"@hasna/loops": patch
---

release(loops): 0.5.8 — contracts storage kit regenerated to @hasna/contracts@0.13.1 (check aligned to the no-STORAGE_MODE contract), optional @hasna/machines pinned to the published 0.2.29, packed-artifact gate hardened (ARN/account-id patterns plus content-level secrets scan with two-sided fixtures), and LoopsClient.healthScan daemonPidPath so daemon probes are hermetic.
