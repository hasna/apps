---
"@hasna/skills": patch
---

Keep the first terminal SQLite SDK credit-reservation reconciliation when separate processes race. Later attempts return the stored status, actual cost and reconciliation timestamp unchanged, matching the existing PostgreSQL adapter contract.
