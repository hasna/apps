---
"@hasna/monitor": patch
---

feat(monitor): monitor-v2 ship wave release — MON-V2-11 hooks native adapter invokes named hooks through the exact `@hasna/hooks` SDK `runHook` surface with classified outcomes and persisted effect receipts (#495, reviewed GO at merged sha de4cb5389); MON-V2-14 contract manifest aligned to contracts kit 0.11.1 with truthful storage/envPrefix declarations and real ./sdk export (#486, reviewed GO at merged sha 3e644f52). Version bump for the release; candidate reviewed and merged per the release gate before publish.
