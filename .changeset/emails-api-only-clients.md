---
"@hasna/emails": patch
---

Require ordinary CLI, TUI, and MCP clients to use the authenticated API. Reject explicit SQLite mail settings before client startup, while retaining explicit storage-library and standalone server compatibility.
