---
"@hasna/emails": patch
---

Connect already-owned domains through authenticated API workflows, using server SES or Resend bindings, shared DNS task receipts, explicit pending verification, and retry-safe registration checks.
