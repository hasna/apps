---
"@hasna/repos": minor
---

Bound secondary aggregate and issue-detection CLI JSON to compact 20-row continuation pages with a 32 KiB response ceiling, opaque mutation-safe cursors, deterministic ordering, stable repository and issue identities, and query-bound issue pagination, while preserving exhaustive legacy arrays behind explicit `--full` and `--all` flags.
