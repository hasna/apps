---
"@hasna/repos": minor
---

Bound secondary aggregate CLI JSON to compact 20-row continuation pages with a 32 KiB response ceiling, while preserving exhaustive legacy arrays behind explicit `--full` and `--all` flags.
