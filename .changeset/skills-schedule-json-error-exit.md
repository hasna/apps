---
"@hasna/skills": patch
---

Return a nonzero exit status when schedule creation fails in JSON mode, matching human output. Invalid cron expressions preserve existing schedule files, and successful creation keeps its current JSON shape.
