---
"@hasna/skills": patch
---

Validate remote version history and individual version responses before returning them. Malformed responses now fail consistently in the SDK and both CLI output formats, while valid empty history, known domain absence, optional fields, and additive server data remain supported.
