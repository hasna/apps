---
"@hasna/repos": minor
---

Add an exact existing-checkout registration command and SDK operation. Plans bind the expected remote, HEAD, branch, index and normal registry identity. Applying inserts one catalog row without workspace bootstrap, hook installation, catalog synchronization or duplicate-row merging.
