---
"@hasna/mementos": patch
---

Bind authenticated SDK requests to their first resolved authority using the shared transport, refresh credential pairs safely, and refuse redirects or local transport changes without stale-key fallback.
