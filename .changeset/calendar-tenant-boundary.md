---
"@hasna/calendar": patch
---

Require an explicitly provisioned authenticated tenant for hosted Calendar access, isolate every domain query and relationship by tenant, and preserve unassigned legacy data until an audited operator migration.
