---
"@hasna/contacts": patch
---

Add `contacts status` (`--json` supported): prints CLI version, resolved API endpoint, storage mode, and contact/company counts, and reports a readable status instead of crashing when the API is unconfigured (hasna/apps#1602).
