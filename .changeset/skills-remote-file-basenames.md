---
"@hasna/skills": patch
---

Preserve valid Unicode and punctuation input filenames through quoted SDK, CLI, and MCP submissions. Match the API's 255-character basename contract while refusing separators, controls, malformed Unicode, dot paths, and duplicate names before network requests.
