---
"@hasna/bench": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/bench with history preserved (import capsule e2e06089, import merge 8939f4752, PR #169); there are no functional changes since 0.0.3 — the delta is the import itself plus the monorepo workspace wiring and the documented absorption fixes (bun-types devDep pin, stale .project.json removal, zod-major-agnostic test assertion). This patch establishes version ownership under the monorepo.
