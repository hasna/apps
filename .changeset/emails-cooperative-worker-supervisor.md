---
"@hasna/emails": patch
---

Add an operator-owned foreground scheduler and sequence supervisor with PostgreSQL ownership leases, atomic generation fences and durable cooperative restart receipts. Report failed or uncertain iterations honestly and reconcile timed-out operations before shutdown.
