---
"@hasna/personalnotes": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/notes with history preserved (import capsule ed16b4ca, import merge aa5dff82); there are no functional changes since 0.1.0 — the delta is the import itself plus the monorepo workspace wiring. This patch establishes version ownership under the monorepo.
