---
"@hasna/emails": patch
---

Allow controlled-send descriptors to reference a reply parent, verify API support before sending, preserve readback without requiring message body files, and expose the generated client through the mandatory `./sdk` alias while retaining `./selfhost`.
