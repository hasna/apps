---
"@hasna/skills": patch
---

Reject unsupported `skills tools validate --profile` values before inspecting the catalog. Preserve the documented `basic` and `all` profiles and the default `all` behavior.
