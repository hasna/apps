---
"@hasna/mementos": minor
---

Change ordinary projects, agents, memory-list, and search JSON from bare arrays to compact continuation envelopes; bound context, injection, and export output with explicit full/exhaustive escapes; make exports truthful, report credentials-file authorities correctly, fail the public SDK closed instead of defaulting to localhost, and route Conversations notifications through the canonical credential-resolved `/v1` authority.
