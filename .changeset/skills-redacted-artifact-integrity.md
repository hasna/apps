---
"@hasna/skills": patch
---

Compute governed artifact hashes and byte counts from the text after redaction so stored metadata and cumulative output limits match the persisted UTF-8 bytes. Refuse missing or non-text bodies before storage instead of fabricating integrity metadata.
