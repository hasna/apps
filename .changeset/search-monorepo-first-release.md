---
"@hasna/search": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/search with history preserved (import capsule 92b60cd4, import merge 1faffc41); there are no functional changes since 0.0.16 — the delta is the import itself plus the monorepo workspace wiring and the documented absorption fixes (bun-types devDep, dashboard build deps hoist, redaction-corpus fixture split). This patch establishes version ownership under the monorepo.
