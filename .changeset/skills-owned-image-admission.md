---
"@hasna/skills": patch
---

Own image-profile configuration and resolved runtime projections, reject ambiguous dependency-layer keys, and capture admission identity, input digest, dependencies, policy, and limits before asynchronous lookups. Return independent admission projections so caller changes cannot rewrite stored runs through service results.
