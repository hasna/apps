---
"@hasna/events": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/events with history preserved (import capsule 718653fff, import merge 46dda5e8b); there are no functional changes since 0.1.15 — the delta is the import itself plus the monorepo workspace wiring and one absorption test-robustness edit. This patch establishes version ownership under the monorepo.
