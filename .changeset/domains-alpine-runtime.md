---
"@hasna/domains": patch
---

Replace the vulnerable Debian slim production image with the pinned multi-architecture Bun Alpine base and exact patched OpenSSL runtime libraries while preserving the zero-CRITICAL/HIGH deployment gate.
