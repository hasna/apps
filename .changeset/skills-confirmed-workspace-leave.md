---
"@hasna/skills": patch
---

Add explicitly confirmed workspace self-leave through the SDK, CLI and MCP. Fresh verification stays bound to the observed membership and selected profile; uncertain outcomes never retry or modify saved credentials.
