---
"@hasna/contacts": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/contacts with history preserved (import capsule 049f22d1, import merge cf4ae2e1f); there are no functional changes since 0.6.35 — the delta is the import itself plus the monorepo workspace wiring. This patch establishes version ownership under the monorepo.
