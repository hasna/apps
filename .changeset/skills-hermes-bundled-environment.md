---
"@hasna/skills": patch
---

Refuse Hermes bundled plugin and skill environment overrides during native discovery and hook checks so unreviewed bundle locations cannot bypass the configured source guards.
