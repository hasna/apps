---
"@hasna/events": patch
---

Add explicit authenticated PostgreSQL event intake with immutable tenant-scoped receipts, operator-bound producers and registered key grants. Provide a shared-credential intake client and a required non-owner PostgreSQL verification gate. Existing local storage and spool exports retain their compatibility behavior.
