---
"@hasna/projects": patch
---

Resolve Projects API credentials through the installed Secrets SDK, including references saved in the credential file. Adopt the shared resolver's ESM loading and keep missing, denied, or empty vault items terminal, so an old literal credential cannot be used instead.
