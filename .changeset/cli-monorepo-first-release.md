---
"@hasna/cli": patch
---

First release from the hasna/apps monorepo. The package was imported from the rescue repository hasna/cli (preservation task baaf03bc) with history preserved (import capsule ba31a5c4, import merge 7475bcd55, PR #168); it carries the owner-ruled unified @hasna/cli 1.0.0 dispatcher (main 2277faea), which was never published from the old repo (npm latest remains the historical pre-rescue 0.1.0 line). The delta is the import itself plus the monorepo workspace wiring. This patch establishes version ownership under the monorepo.
