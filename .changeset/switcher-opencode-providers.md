---
"@hasna/switcher": none
---

Add OpenCode Zen and Go with their full live catalogs and Claude translation to model-native Chat Completions, Responses and Gemini endpoints. Preserve tools, streaming, images, usage, scoped session identity and Retry-After; retain OpenRouter reasoning choices and clear conflicting Claude authentication variables.

Applied with Changesets to 0.2.1 in an isolated single-member staging workspace, together with Switcher's pending Contracts 1.0.2 pin change. Retained as a no-bump release record; the other packages in that dependency wave remain pending.
