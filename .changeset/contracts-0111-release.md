---
"@hasna/contracts": none
---

Release 0.11.1: release-line reconciliation per `contracts-drift-reconcile` (consumed by the 0.11.1 version commit). Registry 0.10.6 -> 0.11.1; no functional changes. This `none` changeset records the release for the versioning-integrity gate (a version change must carry a pending changeset) without queueing a phantom future bump.
