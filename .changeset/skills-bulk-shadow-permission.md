---
"@hasna/skills": patch
---

Honor explicit `port --all --allow-shadow` permission for each imported skill, including the `add` alias. Preserve default official-name refusal, separate overwrite consent and partial-failure reporting.
