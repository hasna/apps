---
"@hasna/events": minor
---

Make standalone and embedded Commander event listings compact and bounded by default with a versioned, canonical, integrity-checked snapshot cursor that binds every field of every snapshotted event and remains stable when events append between pages. Preserve exact unrestricted output behind explicit `--full` values, reject `--full --cursor` instead of silently ignoring the cursor, normalize volatile Bun `.bun` cache hashes out of generated source comments, prove full and filtered install builds are byte-identical, and refresh every tracked runtime and declaration artifact.
