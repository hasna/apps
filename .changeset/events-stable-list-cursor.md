---
"@hasna/events": minor
---

Make event listings compact and bounded by default with an identity-bound snapshot cursor that remains stable when events append between pages. Preserve unrestricted explicit `--full --limit` values, normalize volatile Bun `.bun` cache hashes out of generated source comments, prove full and filtered install builds are byte-identical, and refresh every tracked runtime and declaration artifact.
