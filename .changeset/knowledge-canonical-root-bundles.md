---
"@hasna/knowledge": patch
---

fix(knowledge): regenerate bin/dist bundles — the committed artifacts were stale vs source (canonical project scope + migrate-project-path verb from #362 never rebuilt into the shipped bundles)
