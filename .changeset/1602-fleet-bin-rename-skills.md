---
"@hasna/skills": patch
---

Ship the canonical `skills-serve` server bin (same entrypoint as `bin/server.js`); `skills-server` stays installed as a one-release deprecated alias. Part of the fleet bin-naming wave (hasna/apps#1602).
