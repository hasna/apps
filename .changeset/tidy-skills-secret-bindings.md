---
"@hasna/skills": patch
---

Resolve declared executable secrets through explicit bindings to the selected bundle, profile and local consumer. Add a CLI binding template, configured Secrets SDK resolution, bounded value injection and output redaction. Missing or mismatched grants fail without ambient fallback.
