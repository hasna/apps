---
"@hasna/recordings": patch
---

Keep resolver-backed SDK requests bound to their original authority, pick up same-authority credential rotation, and refuse missing credentials, caller authentication overrides and redirects through the shared transport.
