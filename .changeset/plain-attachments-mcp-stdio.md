---
"@hasna/attachments": patch
---

Register MCP servers with the required --stdio argument in Claude Code, Codex and Gemini. Delegate Codex registration and removal to its native CLI so array values and neighboring TOML tables are not corrupted by regex edits.
