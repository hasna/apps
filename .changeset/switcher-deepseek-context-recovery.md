---
"@hasna/switcher": patch
---

Default Claude Code launches on the official DeepSeek Messages endpoint to the documented compaction window while preserving explicit environment values. Recognize bounded structured context-overflow errors and expose a sanitized native recovery message without reflecting provider error text or retrying another model.
