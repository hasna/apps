---
"@hasna/logs": patch
---

Make `logs list` default to a 25-row, 32 KiB compact identity preview with exact log IDs, UTF-8-safe field truncation, and resumable offsets. Keep full terminal and JSON records behind explicit format selection.
