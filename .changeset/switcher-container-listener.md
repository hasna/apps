---
"@hasna/switcher": none
---

Bind the Docker server to all container interfaces explicitly while preserving the CLI loopback default and authentication requirements.

Applied with Changesets to patch release 0.2.5 in a single-member version staging directory. Retained as a no-bump release record.
