---
"@hasna/uptime": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/uptime with history preserved (import capsule 542e34459, import merge a13fbb504, PR #157); the imported tree carries 0.1.70, which the old repo never published — npm latest is 0.1.69 and the pair is reported live by the npm-parity lane, which auto-files the reconcile task (e5f5ebaa-f3e4-423d-845f-cabd77a8ef55). The delta is the import itself plus the monorepo workspace wiring. This patch establishes version ownership under the monorepo.
