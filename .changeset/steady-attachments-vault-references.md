---
"@hasna/attachments": patch
---

Resolve named Secrets references from the environment or owner-only credential files across CLI, MCP, SDK and task integrations. Install the Secrets SDK, refresh values on every request, and refuse failed lookups or changed authority/reference bindings before dispatch. The generated SDK supports asynchronous credential providers.
