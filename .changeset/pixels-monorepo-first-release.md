---
"@hasna/pixels": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/pixels with history preserved (import capsule c97b04a0, import merge c90b4552c); there are no functional changes since 0.1.0 — the delta is the import itself plus the monorepo workspace wiring and the documented absorption fixes (bun-types devDep, npm-pack dry-run env strip, zod-4-only API migration). This patch establishes version ownership under the monorepo.
