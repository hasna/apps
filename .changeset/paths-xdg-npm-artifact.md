---
"@hasna/paths": patch
---

Honor standard XDG config, data, state, and cache roots beneath higher-precedence Hasna overrides. Preserve native macOS defaults and empty overrides; reject invalid explicit Hasna roots without disclosing their values. Scan the exact npm-packed artifact with strict single-local-tarball validation and lifecycle recursion protection.
