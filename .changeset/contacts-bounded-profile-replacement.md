---
"@hasna/contacts": minor
---

Add bounded compact contact listings with the hosted 500-row ceiling, deterministic `display_name,id` ordering, truthful hosted filter schemas, complete on-demand MCP discovery, and accurate profile setup guidance. Restore `create_contact` forwarding and hosted persistence for `source` and `project_id`, and attach advertised `tag_ids` atomically with the contact and child methods. The explicit full profile and `--full` output retain legacy records without introducing local fallback or alternate API routing.
