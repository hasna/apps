---
"@hasna/clip": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/clip with history preserved (import capsule 6d368060, import merge 2ccedade3, PR #165); there are no functional changes since 0.2.0 — the delta is the import itself, the monorepo workspace wiring, and the contracts-schema alignment (storage.backend per @hasna/contracts >=0.8.7). This patch establishes version ownership under the monorepo.
