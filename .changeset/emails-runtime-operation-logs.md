---
"@hasna/emails": patch
---

Publish tenant-scoped, append-only API worker lifecycle logs and read them through `emails logs tail`, with strict bounds and honest empty states. Preserve operation receipts if a final log write fails.
