---
"@hasna/sandboxes": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/sandboxes with history preserved (import capsule f6ca02598, import merge 2ecb1b18a); there are no functional changes since 1.1.0 — the delta is the import itself plus the monorepo workspace wiring. This patch establishes version ownership under the monorepo.
