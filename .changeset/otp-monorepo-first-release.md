---
"@hasna/otp": patch
---

First release from the hasna/apps monorepo. The package was imported from hasna/otp with history preserved (import capsule fd7b71504, import merge 361cab77e, PR #161); there are no functional changes since 0.1.1 — the delta is the import itself plus the monorepo workspace wiring and the documented absorption fixes (bun-types devDep pin, skipLibCheck). This patch establishes version ownership under the monorepo.
