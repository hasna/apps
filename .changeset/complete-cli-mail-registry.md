---
"@hasna/emails": patch
---

Show all registered addresses and domains by default in the CLI while preserving explicit pagination. Check the complete account registry for unverified addresses, address suggestions, and domain/address name or prefix lookups.
