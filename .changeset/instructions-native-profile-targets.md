---
"@hasna/instructions": patch
---

Render Grok and Devin global AGENTS.md in an explicit native instruction home while preserving existing project targets, ownership checks and rollback. Compile native profile bindings against their flattened loading contract so conditional rules are not silently promoted.
