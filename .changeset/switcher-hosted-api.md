---
"@hasna/switcher": none
---

Extract provider credential onboarding into its focused boundary without changing account, origin, verification, dry-run, or delivery protections. Add the hosted PostgreSQL API with Contracts-signed revocable keys, local credential-backed catalog discovery, metadata-only catalog commits, deployment registration, and fail-closed local-versus-hosted behavior.

Applied to patch release 0.2.3 in this focused release worktree. Retained as a no-bump release record so the reviewed source, hosted rollout, and published package remain attributable without scheduling a duplicate version increment.
