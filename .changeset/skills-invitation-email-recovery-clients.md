---
"@hasna/skills": patch
---

Add explicit invitation email recovery to SDK and CLI, plus a separate two-tool MCP stdio mode. Preserve caller challenge IDs, bounded proof input, uncertain outcomes and unchanged credentials; require ordinary sign-in after acceptance.
