---
"@hasna/skills": patch
---

Keep existing native sessions on their validated pinned profile when the managed hook default changes, while new sessions use the configured profile. Report safe context error codes and the selected profile for genuine conflicts. Preserve fail-closed validation and discard foreign child messages and stderr.
