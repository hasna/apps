---
"@hasna/instructions": patch
---

Refuse Win32 drive-relative OpenCode references before URI classification and remove case, encoded, mixed-separator, drive-root-relative, UNC, file URL, and trailing-dot/space aliases into any Instructions-managed namespace while preserving truly unmanaged paths.
