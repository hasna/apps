---
"@hasna/bridge": patch
---

Sync with hasna/bridge main (delta import): live-owner daemon lock protection, config-derived stop grace, and the 0.7.1 release bump (changelog + version). Content from org commits #14-#17; monorepo #182 content retained and reconciled.
