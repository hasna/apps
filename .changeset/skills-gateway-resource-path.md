---
"@hasna/skills": patch
---

Use the gateway's /skills/v1 resource path while preserving separately selected commercial and custom instances. Reject unsupported internal gateway login before transmitting account input; the independent internal origin and auth adapter remain integration requirements.
