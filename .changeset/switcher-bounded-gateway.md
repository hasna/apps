---
"@hasna/switcher": none
---

Bound inference request buffering to selected and explicitly allowed model context, capped at 64 MiB. Prevent Switcher fallback replay after ambiguous provider connection failure and return a distinct redacted error. Native harness retry behavior remains unchanged.

Applied with Changesets to patch release 0.2.4 in a single-member version staging directory. Retained as a no-bump release record.
