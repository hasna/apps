---
"@hasna/skills": patch
---

Add an explicit capability-gated private source publication SDK, CLI and MCP lifecycle with fresh workspace sessions, exact version UUID comparison, immutable recovery bundles and lost-response reconciliation. Existing registry publishing and private execution semantics remain unchanged.
