{
  "name": "@hasna/billing",
  "version": "0.1.5",
  "description": "Thin agent-facing billing/dunning orchestration over Stripe Billing: subscription lifecycle, decline-code retries, pre-dunning, graduated downgrades, multi-entity invoicing. CLI + MCP + serve over a Hasna-contract store.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "billing": "dist/cli/index.js",
    "billing-mcp": "dist/mcp/index.js",
    "billing-serve": "dist/server/index.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "hasna.contract.json",
    "openapi.json",
    "docker-compose.yml",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "bun build src/cli/index.tsx --outdir dist/cli --target bun --external ink --external react --external chalk --external @modelcontextprotocol/sdk --external hono && bun build src/mcp/index.ts --outdir dist/mcp --target bun --external @modelcontextprotocol/sdk && bun build src/server/index.ts --outdir dist/server --target bun --external hono && bun build src/index.ts --outdir dist --target bun && tsc --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "conformance": "bun run scripts/conformance.ts",
    "verify": "bun run typecheck && bun run test && bun run build && bun run conformance",
    "scan:artifact": "bun run scripts/scan-artifact.ts",
    "prepack": "bun run verify && bun run scan:artifact",
    "prepublishOnly": "bun run prepack",
    "openapi:generate": "bun run src/cli/index.tsx openapi generate --out openapi.json",
    "openapi:check": "bun run src/cli/index.tsx openapi check --path openapi.json",
    "smoke:package": "bun run src/release/package-smoke.ts",
    "dev:cli": "bun run src/cli/index.tsx",
    "dev:mcp": "bun run src/mcp/index.ts --http --port 8891",
    "dev:serve": "bun run src/server/index.ts",
    "postinstall": "node -e \"const fs=require('node:fs'),path=require('node:path'),os=require('node:os');const home=process.env.HOME||process.env.USERPROFILE||os.homedir();const exact=process.env.HASNA_BILLING_HOME||process.env.BILLING_HOME;const dataHome=process.env.HASNA_DATA_HOME;const root=exact&&exact.trim()?path.resolve(exact.trim()):(dataHome&&dataHome.trim()?path.join(dataHome.trim(),'billing'):path.join(home,'.hasna','billing'));try{fs.mkdirSync(root,{recursive:true,mode:0o700});for(const dir of ['config','data','exports','backups','logs','tmp'])fs.mkdirSync(path.join(root,dir),{recursive:true,mode:0o700});}catch{}\""
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hasna/billing.git"
  },
  "homepage": "https://github.com/hasna/billing#readme",
  "bugs": {
    "url": "https://github.com/hasna/billing/issues"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "author": "Andrei Hasna <andrei@hasna.com>",
  "license": "Apache-2.0",
  "dependencies": {
    "@hasna/paths": "0.2.1",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "hono": "^4.6.0",
    "commander": "^13.1.0",
    "ink": "^5.2.0",
    "react": "^18.3.1",
    "chalk": "^5.4.1",
    "zod": "^3.24.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@hasna/contracts": "0.14.2",
    "@types/bun": "^1.2.4",
    "@types/pg": "^8.11.10",
    "@types/react": "^18.3.18",
    "typescript": "^5.7.3",
    "bun-types": "1.3.14",
    "@types/node": "26.0.1"
  }
}
