# Recordings 0.4.1 release validation

This is a **prepared patch candidate**, not an npm publication. Required repository CI and the separately authorized publication workflow remain outstanding.

## Why this patch is necessary

On 2026-09-08, the npm registry reported `@hasna/recordings@0.4.0` published at `2026-09-07T15:58:28.577Z`. Its archive SHA-256 is `72952452ad8babb3bd3146f8f5e9ee537d6da945e2f29686c9e0fcf783ad3542`; its 109-file native tree lacks the reviewed paste-target tracker and `inputEnded()` integration. Registry metadata did not include `gitHead` or provenance attestations. The reviewed prepared 0.4.0 archive has different bytes and cannot be relabeled as that published release.

Fresh `origin/main` at `28f5bd86703d6ccd8a8b45d90159a8f9345c842a` matched all 112 native files of prepared candidate 6 in bytes and modes. An isolated Changesets 2.27.9 metadata workspace applied one Recordings-only patch Changeset to 0.4.1. No unrelated package or pending Changeset was versioned. The repository's version writer updated both native bundle version fields, the SDK generator updated its version header, and Bun updated only the Recordings workspace version in the root lockfile.

## Exact candidate

| Item | Verified value |
| --- | --- |
| Source revision | `9bc43d628f9bbd0a8db01c76c0412e119dcc826f` |
| Archive | `hasna-recordings-0.4.1.tgz` |
| Archive bytes | 950,020 |
| Archive SHA-256 | `a456db766592e41be7534cc152c2222ed4286e5d9422d5c5614cd368b1413e1d` |
| Archive inventory | 267 regular files; 4,014,332 unpacked bytes |
| Prepared receipt SHA-256 | `83dae83d03a6cbf3d6bf38dd1c626ef8ee757df6e618a99e7f6247daaf188132` |
| Native inventory | 112 files, bytes and modes matched to the source revision |
| Native tree SHA-256 | `07752428361055b0d7d6bcfdde11da311c81bca714880cd7fb46a033bbab7403` |

The [prepared native-core receipt](0.4.1-prepared-core.json) was produced by `native-core-receipt.py` with source verification. Its registry URL identifies the intended distribution location; its `prepared` status does not establish publication.

Compared with candidate 6, no archive path or mode changed. Five JavaScript bundles changed only version literals; `package.json` and the native `Info.plist` changed their version fields. The native implementation is unchanged. The package includes `RecordingsLib`, the provider/session protocols, `inputEnded()`, `RecordingPasteTargetTracker`, the receipt generator and universal macOS filesystem guard. This validation did not compile or run Swift again.

## Checks

Bun 1.3.14, npm 11.19.0, macOS 26.3.1 arm64:

- Frozen dependency install, lifecycle hooks disabled: passed. After scoped versioning, only the intended root lockfile version entry changed.
- Required affected build: **43 successful, 43 total**.
- `bun run prepublishOnly`: typecheck passed; **1,400 pass, 114 skip, 0 fail; 1,514 tests across 82 files, each selected once**.
- `npm pack`: version agreement, universal native guard build, package build, release guard and packed-artifact scan passed; **267 members scanned, 0 excluded, 0 unreadable**.
- External packed consumer: **15 actual surface checks** and TypeScript 5.9.3 with `skipLibCheck: false` passed. The [consumer receipt](0.4.1-consumer.json) records versions, checks and boundaries.

The consumer executed the packed CLI, MCP, HTTP server and SDK. All three binaries reported 0.4.1; explicit-local CLI storage was empty; actual MCP stdio initialized, discovered 18 tools and listed recordings; the SDK completed a loopback request without ambient authorization; actual HTTP health/version/OpenAPI/readiness and unconfigured protected-route responses matched their contracts. All 267 package files retained their original bytes and modes afterward.

Consumer dependencies were reused from a separately installed registry dependency tree, then copied into a new owned consumer and the package replaced with this candidate. All 9,877 regular files, 83,301,906 bytes and 11 internal symlinks matched the prior dependency inventory before use. This was not a fresh dependency installation. The existing consumer harness was adapted only for three expected-version literals and to avoid signaling a process group after normal child exit; the receipt records the exact hashes.

The consumer's OS sandbox allowed only the pinned Bun executable, owned writes and loopback networking. Host executable, outside-write and foreign-signal controls passed. The CLI/MCP fixture supplies only the exact own-PID maintenance identity response because sandboxed macOS cannot execute setuid `ps`. Actual package dispatch, storage and transports remain unchanged. No native app, microphone, clipboard, permission, provider or hosted-authentication path was exercised.

## Retained failed attempts and CI boundary

The first package-gate attempt found incomplete preparation: the native guard had not yet been built, the generated SDK header still said 0.4.0, the temporary directory was inside a Git tree and inherited home ACLs, and the reduced tool path selected Bash 3.2 instead of the existing Bash 5 runtime required by the hostile-locale fixture. These failures were retained. Building the guard, regenerating the SDK, selecting a new canonical temporary root outside the repository and pinning Bash 5.3.15 resolved them without assertion or production-code changes. The completed gate used the existing recorder/publication/lifecycle/ordinary partitions and their reviewed child sandboxes; no outer sandbox was nested around them.

`bun run check` was attempted under a separate OS sandbox with real-home, application, Keychain, TCC, host-tool, outside-write and non-loopback access denied. Initial startup refusals were sandbox path-access limitations. An owned source mirror allowed the check to execute: names, dependency direction and the staged secret check passed. Manifest checks could not complete because six versioned validator resolutions still needed public-registry metadata or tarballs, which the sandbox refused even after lifecycle-disabled dependency cache preparation. This is **not a passing full repository check**. Required CI must complete that check and all other required jobs before merge; no refusal was converted into a pass or excluded from the gate.

## Publication remains separate

No npm publish, release tag, global installation, installed-app action or publishing-channel message occurred. Version 0.4.1 returned registry HTTP 404 before preparation. After required checks and explicit publication authorization, use the repository's required announcement/publishing procedure with the reviewed archive. Then compare downloaded registry bytes and integrity with this candidate and produce a separate `published` native-core receipt. Consumers must pin that reviewed published receipt; this prepared receipt cannot satisfy a released dependency.

## Retained log digests

- `affected-build.log`: `ca7f0d80bea9ac2c75810ec2cc6baf2c227a03b82512a7b394bf7d55f3892882`
- `package-prepublish-prepared.log`: `fa7f9a1c13d1eb31cbe34a9fb5834ef8575af302f8551d46ce66c108c2179820`
- `package-pack.log`: `5b054088a44d0ebae7097bb226edc72dc38a45a3ce13d102498cb715a645c9bc`
- `root-check-mirror-cached.log`: `9440f19cd92ebca39691eaf8913d4f31a64841612c112e753f52658464126605`
