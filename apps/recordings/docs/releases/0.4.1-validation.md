# Recordings 0.4.1 release validation

Candidate 2 is a prepared patch release. Publication, registry-byte verification and
installed-app acceptance remain separate. The published 0.4.0 archive lacks the
reviewed frozen paste-target and provider input-ended fixes; 0.4.1 provides a new
immutable version containing the reviewed core.

## Current candidate

| Item | Verified value |
| --- | --- |
| Packed source | `490b64f360e8baa0b4d49d1e17b3140e064242e5` |
| Archive | `hasna-recordings-0.4.1.tgz`,950,710bytes |
| Archive SHA256 | `a7a342c2a206e1ee2f06c957d6a654d7d54f92cdad626bc89958593f6cb73d68` |
| Prepared receipt SHA256 | `4a4afb0d9e816734a39764a71cee6eb22d1bab9425f411b4e8e3e707aed7de7b` |
| Archive inventory |267 regular files;4,018,024 expanded bytes |
| Native inventory |112 files, bytes and modes matched to source |
| Native tree SHA256 | `ffbd54eec6031d89a684164454946a083fff428403558c737f223c604324e349` |

The [prepared core receipt](0.4.1-prepared-core.json),
[consumer receipt](0.4.1-consumer.json) and
[pack evidence with log/harness hashes](0.4.1-candidate2-validation.json) bind this
exact candidate. The distribution status remains `prepared`.

## Packaged fixture correction

The earlier native CI compiled but a test expected pending selection capture
without enabling intent detection. Production correctly skipped that callback,
so the fake empty recording reached `No audio captured` before the assertion.
The failure reproduced in 0.205 seconds under owned OS confinement.

The tests-only repair explicitly sets the fixture's intent mode, requires entry
into its callback with the frozen PID, and retains the existing 150 ms finalizing
assertion while the callback is unresolved. It covers disabled intent detection,
restores the single preference synchronously, and disables global handlers at
initialization. All 13 focused start/warm-up tests passed in 0.313 seconds, and the
native application and test targets compiled. These durations are fixture
observations, not microphone or paste performance measurements.

Compared with candidate 1, exactly one packed file changes:
`RecordingsTests/RecordingStartTimingTests.swift`,15,247to18,939bytes. All 267 paths
and modes are unchanged; the other 266 files, including every production native
file, JavaScript bundle and universal filesystem addon, are byte-identical.
Candidate1 and its receipts are preserved. The applied `none` Changeset continues
to record the already-applied0.4.1 bump without scheduling another version.

## Validation and limits

Actual npm 11.19.0 pack with Bun 1.3.14 passed the maintained prepack lifecycle:
universal native guard, version agreement, build, release guard, and267-member
artifact scan with zero exclusions or unreadable files. It used a stripped
environment and owned HOME/TMP/cache; the pack process had no outer OS sandbox.

The unchanged confined consumer passed all 15 actual CLI/MCP/HTTP/SDK checks,
including 18 MCP tools, and strict TypeScript 5.9.3 with `skipLibCheck: false`.
Every package byte and mode remained unchanged afterward. Its reused dependency
tree matched 9,877 files, 83,301,906 bytes and 11 internal symlinks before use; this was
not a fresh dependency install. Network access was loopback-only, and the existing
own-PID maintenance identity response remained an explicit fictional fixture.

The broad Bun suites were not repeated locally for the isolated Swift test change.
Earlier candidate 1 macOS results were 1,400 pass / 114 skip / 0 fail and independent Linux
results 1,503 pass / 11 skip / 0 fail. Fresh required CI and the complete native observation
must be checked on the final PR head before release readiness is claimed. Earlier
CLI deadline failures fluctuated across CI runs and remain disclosed in the PR.
No production timeout or assertion was weakened.

[Historical candidate 1 validation](0.4.1-candidate1-validation.md) preserves initial
preparation failures, the incomplete confined repository check and earlier exact
artifact evidence. No npm publication, release tag, installed-app, microphone,
clipboard, permission or provider action is part of this preparation. Publication
requires the authorized repository notice procedure, exact registry-byte checking
and a new `published` receipt; a prepared receipt cannot satisfy the product pin.
