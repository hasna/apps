# 0.4.0 release validation

Status: **prepared, not published**. The complete Recordings release gate passes
at source `c2db5539e5b1627cd2d5c9d73f8c8c0d4d4a6380`. This record distinguishes
package validation from publication and acceptance of an installed application.

## Version and included source

The public registry reported `0.3.13` (published 2026-08-25) before preparation;
`0.4.0` was absent. The five Recordings Changesets pending at preparation included
one minor and four patches, selecting **0.4.0**. Only those Changesets were consumed.
The existing manual Unreleased S3 note was also moved into this release because
its source is already included. Other member versions are unchanged.

Included functionality spans the contracts credential/authority resolver, S3
audio artifacts, native recorder/history/settings/playback/paste improvements,
and the exported `RecordingsLib` provider API. The release packages all of these
changes. Native consumers require Swift 6.2 and macOS 26.

## Complete package gate

On 2026-09-07, Bun **1.3.14** completed `bun run prepublishOnly`, including
typechecking and the strict partitioned release suite:

```text
release-suite-gate: PASS - 1378 pass, 114 skip, 0 fail; 1492 tests across 80 files, each selected once
```

| Group | Pass | Selected skip | Fail | Selected tests |
| --- | ---: | ---: | ---: | ---: |
| Recorder | 16 | 0 | 0 | 16 |
| Publication | 42 | 93 | 0 | 135 |
| Lifecycle | 15 | 12 | 0 | 27 |
| Ordinary | 1305 | 9 | 0 | 1314 |
| Total | **1378** | **114** | **0** | **1492** |

The 80-file count deduplicates the lifecycle file shared by two complementary
groups. Name-filtered registrations are excluded from the aggregate; the 114
selected skips remain visible. Every group supplied complete JUnit and console
receipts, and every selected test had exactly one owner. No failing-test allowlist
or quarantine entry was added. See [suite orchestration](0.4.0-suite-orchestration.md)
for the process boundaries and all-tests versus gated inventory contract.

Retained evidence: `recordings-complete-release-gate.log` and the
`recordings-release-gate-pTvQAn` report directory containing each group's XML/log
and the combined `suite.log`. The required affected build also passed **43 of 43
tasks**. The final repository `bun run check` rerun completed with **exit 0** at
the same unchanged source: names, dependency direction, manifests, tarball and
deployment checks, **147 standard tests / 560 assertions**, and matching frozen
lockfiles. That separate result is retained in `recordings-release-awake-root-check.log`.

## Fourth prepared archive

The current archive is `hasna-recordings-0.4.0.tgz` from the fourth candidate.
Its recorded source is the exact revision above. The archive checksum and file
counts were independently read back from the retained bytes:

| Field | Value |
| --- | --- |
| Distribution status | `prepared` |
| Archive size | **941754 bytes** |
| Archive files | **267**, including **112 native package files** |
| Archive SHA-256 | `93d2598c274aeb5f264256cabf8d7d3ce41aa42792de47a15a31a8b54d093d13` |
| Native tree SHA-256 | `7168982b7bd3f3ac176b1110941a6a2acb661aba570c2d259120cff40ba4f600` |
| Prepared receipt | `recordings-core-0.4.0.prepared.json` |
| Receipt SHA-256 | `ad142b4b5334ad4f2c3b9844ae86699443c1fcb2cb98a4b770372a10d8a0f86e` |
| Native package / product | `src/native/Recordings` / `RecordingsLib` |

The receipt's registry URL names the intended immutable distribution location;
its presence is not evidence that those bytes have been published. The earlier
276-file, 109-native-file third candidate is superseded by this archive. All 112
native files also matched the recorded public source revision in bytes and modes.

A separate downstream native build consumed the packaged core and passed
**23 confined integration tests in 7 suites**. This is controlled fixture evidence,
not a claim of real microphone capture, delivery into another application, or
hosted-provider acceptance.

The fresh fourth-candidate npm consumer passed **15 surface checks** and strict
**TypeScript 5.9.3** checking with `skipLibCheck: false`, on macOS 26.3.1 arm64
and Bun 1.3.14:

- CLI, MCP and serve binaries reported **0.4.0**. The explicit local CLI listing
  returned the fixture's empty recording list.
- The actual MCP stdio process completed initialization, discovered **18 tools**,
  and answered a recording-list tool call.
- The SDK sent an actual loopback request without inherited authentication.
- The actual serve process returned 200 for `/health`, `/version`,
  `/openapi.json` and default SQLite `/ready`; the unconfigured protected
  `/v1/recordings` and `/mcp` routes returned 503.

The consumer used a new owned copy of previously installed external registry
dependencies, including Contracts 1.0.2 and Events 0.1.11, with Recordings replaced
from this verified archive and lifecycle scripts disabled. Dependency resolution
dates to that earlier installation. The packaged CLI, MCP, serve and SDK code
was unchanged.

The Darwin sandbox refused setuid `ps` execution. For CLI/MCP only, a preload
answered the exact own-PID identity lookup with a fictional maintenance-lease
identity. Every other `child_process` entry point was denied. OS controls limited
network access to loopback and blocked host application/user-home reads;
negative controls verified rejection of outside writes, six host tools and
signals to a separately owned process. These checks exercise the packaged
surfaces under that disclosed fixture boundary; they do not establish real
macOS recording delivery or hosted-provider acceptance.

## Historical independent checks

Earlier candidates passed these checks during preparation; they are retained as
history rather than substituted for fresh fourth-candidate results:

- Bun **1.3.14**: package typecheck and all three version sites passed at 0.4.0.
- Required affected build: **43 successful, 43 total**.
- Receipt regression suite: **10 pass, 0 fail, 20 assertions**. Controls reject
  ambient Git archive modes, changed source/package metadata, missing library/provider files, unsafe archive
  entries, wrong package identity, and registry metadata or byte mismatches.
- Actual macOS `npm pack` ran the native filesystem guard build, package build,
  version check, release scan, and artifact scan successfully. The third archive had
  **276 files**, including **109 native package files**, without Python caches.
  It incorporated the descriptor helpers and rebuilt C guard. Its native files
  were byte- and mode-identical to the tested second candidate; the public CLI,
  MCP, serve, and SDK runtime files were unchanged between those two candidates.
- Extracted archive: **26 Swift tests in 5 suites passed**, including live injected
  PCM ordering/pause/final WAV identity, cancellation, isolated preferences,
  service routing, and honest paste evidence. The three expected assertion
  integrity issues are deliberate negative controls, not ignored product errors.
- An external SwiftPM executable imported and linked `RecordingsLib` from the
  extracted archive, including the public engine initializer and provider/session
  protocols. Its inert consumer ran successfully without starting capture.
- A separate npm project installed the archive and declared registry dependencies
  with lifecycle scripts disabled. TypeScript **5.9.3**, `strict: true`, and
  `skipLibCheck: false` checked the SDK consumer. Its loopback SDK request,
  explicit local CLI listing, and MCP stdio initialization/discovery/listing passed.
  All three binaries reported 0.4.0. The installed serve binary returned version and
  health over loopback and refused an unconfigured protected route with 503.

The first strict consumer attempt used TypeScript 5.8.2 with newly resolved Node
26 declarations and hit an upstream DOM TextEncoder/TextDecoder declaration
incompatibility; 5.9.3 checks the same declarations without suppressing errors.

## Initial failures and completed repairs

The initial `bun run release-suite-gate` completed with **1229 pass, 115 skip, 105 fail**, over
1449 tests in 78 files. No failures were allowlisted or removed. The following
counts are distinct failing test names, grouped from their actual errors. They
are historical; the integrated complete package gate now passes as recorded above.

The reviewed descriptor, trigger, and journal repairs cover **44 of those original
failures**. Integrated, confined reruns pass: descriptor binding **40 pass, 0 fail,
197 assertions**; trigger diagnosis **32 pass, 1 existing strace-only skip, 0 fail,
139 assertions**; journal compatibility **39 pass, 1 existing Linux-only skip,
0 fail, 306 assertions**. The journal suite now includes a symlinked-home rejection
control. Production trust checks and macOS executable pinning remain enforced.
The former **61 remaining failures** was an intermediate count. Subsequent
isolated repairs and the completed full rerun supersede that active blocker.

Descriptor and journal runs deny network access, unapproved process execution,
and writes outside their private fixture roots, with positive and negative
controls. Trigger children use the guarded test launcher and its independent
OS process restrictions, including a real defaults-execution rejection control.
An initial attempt to wrap that launcher in a second macOS sandbox failed before
fixture execution because nested sandbox application is refused; the separate
reruns above used the supported confinement paths. The final full gate also uses
the supported boundaries, as recorded in the orchestration document.

| Group | Failures | Observed cause and bounded repair |
| --- | ---: | --- |
| Native descriptor extraction | 1 | Repaired: retained descriptor-relative filesystem operations replace `/dev/fd` traversal; caller descriptor ownership and symlink/substitution controls pass. |
| Trigger write/read fixtures | 5 | Repaired: isolated launcher remaps only owned fixture tools and blocks host processes; production Darwin pinning and exact fixture-write arguments pass. |
| Install journal compatibility | 38 | Repaired: canonical owned fixture roots reach the intended recovery assertions; unsafe symlinked-home rejection remains covered. |
| Finalized artifact installer | 10 | Repaired: confined local fixtures reach supported paths; obsolete shell-release requests assert mandatory broker refusal without mutation. |
| Identity migration | 11 | Repaired: confined copied-script tools exercise the current identity guard and approval decision table. |
| Signed artifact build | 10 | Repaired: owned committed Git fixtures establish provenance before simulated signing checks; strict native-guard export verification includes the reviewed descriptor helper. |
| Runtime smoke identity | 13 | Repaired: confined launch/process evidence and owned cleanup replace host `open`/PID access; the production readiness window is preserved. |
| Artifact signing manifest | 3 | Repaired: confined signing evidence replaces real codesign on invalid fixtures. |
| Release publication | 4 | Repaired: canonical private roots reach no-replace/recovery checks while substitution controls remain active. |
| Installer maintenance | 3 | Repaired: supported local-only fixtures exercise marker acquisition, reader drain and cleanup; broker enforcement is unchanged. |
| Tailscale snapshot | 3 | Repaired: canonical owned snapshots and portable physical-path builtins preserve ownership, symlink and signature checks. |
| CLI app status/install | 4 | Repaired: confined platform/filesystem probes replace host discovery; an absent installation has no invented permission subject. |

Three representative failures reproduce unchanged at public main
`60deb91c050f4ff3c95a646be40e7bf80de7b3dc`: descriptor extraction, a dead-trigger
check incorrectly returning zero in the fixture, and shell installer recovery
refused by the broker guard. Those pre-isolation invocations are historical and
must not be reused against host tools. Repair details and limits are in the
[identity](0.4.0-identity-fixtures.md), [signing](0.4.0-signing-fixtures.md),
[installer/publication](0.4.0-installer-publication-fixtures.md), and
[CLI/recorder](0.4.0-cli-host-fixtures.md) records. Real signing and notarization
are not claimed.

Earlier repository checks remain failed observations: sparse task worktrees
lacked unrelated dependencies, and a Contracts hostname-guard failure retained
only a truncated diagnostic tail. Its isolated rerun passed, but its cause was
not established. A later Contracts CLI test timed out at **275120.53ms** against
its unchanged **120000ms** limit while the host entered recorded idle sleep for
**277 seconds** (14:37:44–14:42:21 +0300 on 2026-09-07). The unchanged repository
check then passed with exit 0 under a process-scoped idle-sleep assertion. No
timeout or assertion was weakened; the sleeping run remains failed evidence.

## Publication boundary

Review and merge the release changes before separately authorized publication.
Generate a fresh reviewed archive after any packaged-byte change. A prepared
receipt cannot authorize publication and cannot satisfy a released dependency.
After publishing the exact accepted archive, `native-core-receipt.py
--verify-registry` must match the registry metadata and downloaded bytes before
producing a separate `published` receipt. Consumers must pin that reviewed
receipt through trusted configuration. No npm publish, release tag, application
installation, real capture-to-paste delivery, or Google sign-in acceptance is
established by this preparation.
