{
  "note": "Measured failing checks from `contracts repo-conformance`. This file is a ratchet, not an excuse: `bun run contract-gap` fails when the real failure set differs from this one in EITHER direction, so a new violation is caught and a fixed violation forces the baseline down. Regenerate with `bun run contract-gap --update` and land the change with the fix that earned it.",
  "contractsVersion": "0.8.5",
  "reported": [
    {
      "id": "self_host_artifact",
      "detail": "service-class repos require one self-host deployment artifact: docker-compose.yml, docker-compose.yaml, compose.yml, compose.yaml, Dockerfile"
    },
    {
      "id": "service_api_topology",
      "detail": "a supported API surface is required"
    },
    {
      "id": "storage_capabilities",
      "detail": "storage.pgTestGate is required to prove live PostgreSQL support"
    },
    {
      "id": "surface_matrix",
      "detail": "missing supported surface declarations or eligible waivers: api, sdk"
    }
  ],
  "behindManifestGate": [
    {
      "id": "self_host_artifact",
      "detail": "service-class repos require one self-host deployment artifact: docker-compose.yml, docker-compose.yaml, compose.yml, compose.yaml, Dockerfile"
    },
    {
      "id": "service_api_topology",
      "detail": "a supported API surface is required"
    },
    {
      "id": "storage_capabilities",
      "detail": "storage.pgTestGate is required to prove live PostgreSQL support"
    },
    {
      "id": "surface_matrix",
      "detail": "missing supported surface declarations or eligible waivers: api, sdk"
    }
  ]
}
