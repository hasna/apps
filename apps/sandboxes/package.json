{
  "name": "@hasna/sandboxes",
  "version": "0.0.0-v1-managed-adapters",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist/index.js"
  ],
  "scripts": {
    "test": "bun run test:hermetic",
    "test:hermetic": "tmp=$(mktemp -d); trap 'rm -rf \"$tmp\"' EXIT; env -i PATH=\"$PATH\" HOME=\"$tmp\" XDG_CONFIG_HOME=\"$tmp/config\" TMPDIR=\"$tmp\" TZ=UTC bun test tests/managed-adapters",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/adapters/managed/index.ts --outdir dist --target node --format esm --external e2b --external @daytona/sdk",
    "audit": "bun audit",
    "prepack": "bun run build"
  },
  "dependencies": {
    "@daytona/sdk": "0.193.0",
    "e2b": "2.31.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.5",
    "typescript": "5.9.3"
  },
  "overrides": {
    "@opentelemetry/core": "2.8.0",
    "protobufjs": "8.6.5"
  }
}
