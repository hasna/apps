{
  "name": "@hasna/sandboxes",
  "version": "1.0.0",
  "description": "Provider-neutral, fenced sandbox lifecycle primitives for local and self-hosted operation",
  "type": "module",
  "bin": {
    "sandboxes": "dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "schemas",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build src/cli.ts src/index.ts --outdir dist --target bun --format esm --sourcemap=external && tsc --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:hermetic": "bun test tests/hermetic.test.ts tests/validation.test.ts tests/service.test.ts tests/storage-conformance.test.ts",
    "pack": "bun pm pack --quiet"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "license": "Apache-2.0",
  "author": "Andrei Hasna <andrei@hasna.com>",
  "engines": {
    "bun": ">=1.2.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.8",
    "typescript": "5.9.3"
  }
}
