{
  "name": "@hasna/sandboxes",
  "version": "0.0.0-v1-managed-adapters",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist/index.js",
    "dist/adapters/managed/e2b-guest-broker-v1.py",
    "dist/types"
  ],
  "scripts": {
    "test": "bun run test:hermetic",
    "test:hermetic": "tmp=$(mktemp -d); trap 'rm -rf \"$tmp\"' EXIT; env -i PATH=\"$PATH\" HOME=\"$tmp\" XDG_CONFIG_HOME=\"$tmp/config\" TMPDIR=\"$tmp\" TZ=UTC bun test tests/managed-adapters",
    "test:node": "bun run build && node tests/node/managed-promise-boundary.mjs",
    "test:types": "bun run build && tsc -p tests/types/tsconfig.json",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/adapters/managed/index.ts --outdir dist --target node --format esm --external e2b --external @daytona/sdk && tsc -p tsconfig.build.json && bun scripts/fix-declaration-imports.ts && mkdir -p dist/adapters/managed && cp scripts/e2b-guest-broker-v1.py dist/adapters/managed/e2b-guest-broker-v1.py",
    "audit": "bun audit",
    "prepack": "bun run build"
  },
  "dependencies": {
    "@daytona/sdk": "0.193.0",
    "@types/ws": "8.18.1",
    "e2b": "2.31.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.5",
    "typescript": "5.9.3"
  },
  "overrides": {
    "@opentelemetry/core": "2.8.0",
    "protobufjs": "8.6.5"
  }
}
