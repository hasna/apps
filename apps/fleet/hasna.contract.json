{
  "schema": "hasna.service_contract.v1",
  "name": "fleet",
  "class": "cli-with-store",
  "contractVersion": "v1",
  "kitVersion": "0.11.1",
  "description": "Read-only AgentOps control tower CLI + MCP + serve. SQLite at ~/.hasna/fleet/fleet.db is authoritative when no DATABASE_URL is configured; with one, reads/writes to fleet's own config tables go directly to the shared cloud Postgres. Fused upstream observability data is read-only and never persisted as source-of-truth.",
  "bins": [
    "fleet",
    "fleet-mcp",
    "fleet-serve"
  ],
  "storage": {
    "envPrefix": "HASNA_FLEET_",
    "aliasEnvPrefix": "FLEET_",
    "databaseUrlSecretRef": "hasna/oss/fleet/database-url",
    "sqlitePath": "~/.hasna/fleet/fleet.db",
    "backend": "sqlite"
  }
}
