{
  "name": "@hasna/orgs",
  "version": "0.1.2",
  "description": "Open organization graph and delegation-context layer for agentic systems",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "orgs": "dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./store": {
      "types": "./dist/store.d.ts",
      "import": "./dist/store.js"
    },
    "./snapshot": {
      "types": "./dist/snapshot.d.ts",
      "import": "./dist/snapshot.js"
    }
  },
  "files": [
    "dist",
    "docs",
    "examples",
    "LICENSE",
    "README.md",
    "postinstall.js"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "bun run clean && bun build src/cli.ts --outdir dist --target bun && bun build src/index.ts src/store.ts src/snapshot.ts src/types.ts --outdir dist --target bun && tsc --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "verify:release": "bun run typecheck && bun test && bun run build && npm pack --dry-run --ignore-scripts",
    "verify:published": "npm view @hasna/orgs version --json",
    "dev:cli": "bun run src/cli.ts",
    "prepublishOnly": "bun run verify:release",
    "postinstall": "node postinstall.js",
    "scan:artifact": "bun scripts/artifact-scan.ts",
    "prepack": "bun run build && bun run scan:artifact"
  },
  "keywords": [
    "orgs",
    "organization-graph",
    "delegation",
    "agentic",
    "ai-agents",
    "teams",
    "cli"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hasna/orgs.git"
  },
  "homepage": "https://github.com/hasna/orgs",
  "bugs": {
    "url": "https://github.com/hasna/orgs/issues"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "author": "Hasna",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3",
    "@hasna/contracts": "1.0.0"
  }
}
