/** Package version, kept in sync with package.json. */
export const VERSION = "0.1.0";

/**
 * reveal.js version this SDK targets. Used as the default CDN pin for
 * standalone HTML exports and the React viewer's stylesheet.
 */
export const REVEAL_VERSION = "6.0.1";

/** Current on-disk / serialized deck schema version. */
export const DECK_SCHEMA_VERSION = 1;
