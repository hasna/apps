{
  "schema": "hasna.service_contract.v1",
  "name": "secrets",
  "class": "service",
  "contractVersion": "v1",
  "kitVersion": "0.11.1",
  "description": "Agent-friendly secrets vault: CLI + MCP + HTTP serve + SDK. SQLite at ~/.hasna/secrets/vault.db is authoritative when no DATABASE_URL is configured; with one, all reads/writes hit the shared cloud Postgres. The deployed secrets-serve enforces strict API-key auth (@hasna/contracts).",
  "bins": [
    "secrets",
    "secrets-mcp",
    "secrets-serve"
  ],
  "storage": {
    "envPrefix": "HASNA_SECRETS_",
    "aliasEnvPrefix": "SECRETS_",
    "databaseUrlSecretRef": "hasna/oss/secrets/database-url",
    "sqlitePath": "~/.hasna/secrets/vault.db",
    "backend": "postgresql"
  },
  "metadata": {
    "serve": {
      "healthPath": "/health",
      "readyPath": "/ready",
      "versionPath": "/version",
      "apiBase": "/v1",
      "auth": "api-key",
      "signingSecretEnv": "HASNA_SECRETS_API_SIGNING_KEY"
    }
  }
}
