{
  "name": "@hasna/cli",
  "version": "1.0.0",
  "description": "Unified dispatcher for @hasna apps — spawns installed app CLIs with byte-exact argv passthrough",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "hasna": "dist/cli/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "bun build src/cli/index.ts --target bun --outfile dist/cli/index.js && chmod +x dist/cli/index.js",
    "test": "bun test"
  }
}
