{
  "schema": "hasna.service_contract.v1",
  "name": "ui",
  "class": "library",
  "contractVersion": "v1",
  "kitVersion": "0.11.1",
  "description": "Offline-first UI design toolkit: mirror the ui.sh design skill locally and preview variants with a self-contained, dependency-free setup.",
  "hosting": [
    "user-hosted"
  ],
  "bins": [
    "ui"
  ],
  "serviceSurfaces": [
    {
      "name": "ui-cli",
      "kind": "cli",
      "status": "supported",
      "bin": "ui",
      "authMode": "local-only"
    },
    {
      "name": "ui-lib",
      "kind": "sdk",
      "status": "supported",
      "authMode": "none",
      "exportSubpath": "."
    }
  ],
  "metadata": {
    "conformance": {
      "waivedSurfaces": [
        {
          "kind": "api",
          "reason": "No supported HTTP API product surface: src/server.ts is a local preview/demo server (GET /, /ui-picker.js, /fetch shim) without contract-standard /ready, /version, or published OpenAPI, and is not shipped as a service."
        },
        {
          "kind": "mcp",
          "reason": "No MCP server surface is shipped; src/mcp-client.ts is a client for the external ui.sh MCP only."
        }
      ]
    },
    "release": {
      "artifactScan": {
        "script": "scan:artifact"
      }
    }
  }
}
