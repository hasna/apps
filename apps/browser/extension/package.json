{
  "name": "@hasna/browser-extension",
  "version": "0.5.36",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && cp manifest.json dist/manifest.json && cp dist/src/popup/popup.html dist/popup.html && rm -rf dist/src",
    "test": "bun test src/**/*.test.ts"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "typescript": "^5.7.3",
    "happy-dom": "^15.11.7"
  }
}
