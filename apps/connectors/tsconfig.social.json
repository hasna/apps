{
  "comment": "Emits declarations for the stateless @hasna/connectors/social entry. social imports connector transports from connectors/* (outside src). Rooted at the repo so both src/social and connectors/* are in-tree; declarations land under dist/.types/ (the ./social export points at dist/.types/src/social/index.d.ts). Runtime JS is produced separately by `bun build`.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "declaration": true,
    "emitDeclarationOnly": true,
    "outDir": "./dist/.types",
    "rootDir": ".",
    "types": ["bun-types"]
  },
  "include": ["src/social/**/*.ts", "connectors/x/src/**/*.ts", "connectors/bluesky/src/**/*.ts"],
  "exclude": ["**/*.test.ts", "node_modules", "dist", "bin"]
}
