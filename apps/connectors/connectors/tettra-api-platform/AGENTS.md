# connect-tettra-api-platform

Bearer-token API connector for Tettra Api Platform (`api.tettraapiplatform.com`).

- Config: `~/.hasna/connectors/tettra-api-platform/`
- Env prefix: `TETTRA_API_PLATFORM_`
- Not the same as Tettra KB (`app.tettra.co` / `support.tettra.com`)
