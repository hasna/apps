# Unit.sh API Configuration
# Get your API token from https://app.unit.co/settings/api-tokens

# API token (required)
UNIT_API_TOKEN=

# Environment: sandbox (default) or production
UNIT_ENVIRONMENT=sandbox
