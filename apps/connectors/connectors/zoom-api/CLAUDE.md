# Zoom Api Connector

This package targets the Zoom Api REST API under `https://api.zoomapi.com/v1`.

Authentication uses `Authorization: Bearer <api key>`.

This connector is distinct from `connectors/zoom` (Zoom.us at `api.zoom.us` with Server-to-Server OAuth).
