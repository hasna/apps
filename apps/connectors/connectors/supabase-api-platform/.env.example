# Supabase Management API credentials
# Required: set the Supabase Management API personal access token.

# Optional: override API base URL (default https://api.supabase.com/v1)
# Set a non-default Management API base URL when needed.
