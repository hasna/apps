# Stripe Apps API credentials
# Copy this file to .env and fill in your own values.

# Required: API key used as a Bearer token (Authorization: Bearer <key>)
STRIPEAPPS_API_KEY=[REDACTED]

# Optional: override the default API base URL (https://api.stripeapps.com/v1)
# STRIPEAPPS_BASE_URL=[REDACTED]
