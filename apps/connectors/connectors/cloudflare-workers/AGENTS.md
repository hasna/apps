# Cloudflare Workers Connector

Follow the repository root agent instructions. This connector uses Cloudflare Workers REST API endpoints and must not add browser automation dependencies.

## Checks

```bash
bun run typecheck
bun run build
bun run ./src/cli/index.ts --help
```
