# Terminus API key (required)
# Create keys in your Terminus account settings.
TERMINUS_API_KEY=your-api-key-here

# Optional: override API base URL (default https://api.terminusapp.com)
# TERMINUS_BASE_URL=https://api.terminusapp.com

# Optional: auth mode — basic (default) or bearer
# TERMINUS_AUTH_MODE=basic
