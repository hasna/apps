{
  "name": "@hasna/connect-zerobounce",
  "version": "0.0.1",
  "description": "ZeroBounce email validation, bulk verification, scoring, and enrichment connector",
  "type": "module",
  "bin": {
    "connect-zerobounce": "./bin/index.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun && bun build ./src/cli/index.ts --outdir ./bin --target bun && tsc --emitDeclarationOnly --declaration --outDir ./dist --rootDir ./src --target ESNext --module ESNext --moduleResolution bundler --esModuleInterop --strict --skipLibCheck --types bun-types ./src/index.ts",
    "dev": "bun run ./src/cli/index.ts",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build",
    "release": "bun run scripts/release.ts",
    "release:dry": "bun run scripts/release.ts --dry-run"
  },
  "keywords": [
    "zerobounce",
    "email-validation",
    "email-verification",
    "deliverability",
    "connector",
    "cli"
  ],
  "author": "Hasna",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "chalk": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hasna/connect-zerobounce.git"
  }
}
