# Default webhook destination URL (optional if set per command)
WEBHOOKS_DEFAULT_URL=https://example.com/webhook

# HMAC signing secret for X-Webhook-Signature headers (optional)
WEBHOOKS_SIGNING_SECRET=your-signing-secret-here
