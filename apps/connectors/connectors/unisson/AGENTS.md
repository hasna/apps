# AGENTS.md

`@hasna/connect-unisson` — TypeScript CLI and library for the Unisson Runner API.

## Structure

```
src/
├── api/       # HTTP client + agents/tasks/knowledge modules
├── cli/       # Commander CLI
├── types/     # TypeScript types
└── utils/     # config + output
```

## Auth

API key Bearer token. No browser-use dependency.
