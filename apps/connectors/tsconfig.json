{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "declaration": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "jsx": "react-jsx",
    "types": ["bun-types", "react"]
  },
  "include": ["src/**/*"],
  "comment-social": "src/social is the stateless @hasna/connectors/social entry; it imports connector transports from connectors/* (outside rootDir) and is typechecked + built via tsconfig.social.json so the main rootDir-constrained declaration emit stays clean.",
  "exclude": ["node_modules", "dist", "bin", "connectors", "src/social"]
}
