# Dependencies
node_modules/

# Build outputs
dist/
bin/

# Bun (root workspace member only — the standalone dashboard commits its own lock)
/bun.lock
# dep-connectors-aws-1: the aws connector's lockfile is tracked so the
# standalone install's @aws-sdk resolution is frozen, not re-resolved.
!connectors/aws/bun.lock

# OS
.DS_Store

# IDE
.idea/
.vscode/

# Environment
.env
.env.local

# Test coverage
coverage/

# Per-connector test HOME sandboxes (bun install cache blobs land here)
.test-home/

# Local connector installs
.connectors/
.playwright-mcp/

# Local tool artifacts
.codewith/
.takumi/

# Duplicate connector build output
connectors/connect-imessage/
