# @hasna/security

Legacy command aliases for `@hasna/shield`.

Use `@hasna/shield` for new installs. This package keeps existing `security`,
`security-mcp`, and `security-serve` commands working by forwarding to the
matching `shield`, `shield-mcp`, and `shield-serve` binaries.
