services:
  serve:
    image: oven/bun:1
    command: ["bunx", "--package", "@hasna/shield", "shield-serve"]
    environment:
      SECURITY_DB: /data/shield.db
    volumes:
      - shield-data:/data
volumes:
  shield-data:
