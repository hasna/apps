{
  "schema": "hasna.service_contract.v1",
  "name": "docs",
  "class": "library",
  "contractVersion": "v1",
  "kitVersion": "0.11.2",
  "description": "Headless rich-text document SDK over the ProseMirror/TipTap JSON model. Primary surface is a browser React <Editor> (@hasna/docs/react) plus a framework-agnostic, dependency-free document core (createDocument/loadDocument, Markdown/HTML/JSON import-export, outline + word count, programmatic edits/steps). A `docs` CLI performs headless conversions and analysis over files. MCP + HTTP serve + Postgres persistence are optional scaffolding not yet shipped in this v1.",
  "bins": [
    "docs"
  ],
  "metadata": {
    "conformance": {
      "waivedSurfaces": [
        {
          "kind": "api",
          "reason": "Docs ships no HTTP API surface: it is a headless document SDK (ProseMirror/TipTap JSON model) with a CLI and a React component library; HTTP serve is not part of the shipped v1."
        },
        {
          "kind": "mcp",
          "reason": "Docs ships no MCP surface: there is no MCP server bin or tools surface in the package."
        }
      ]
    },
    "release": {
      "artifactScan": {
        "script": "scan:artifact"
      }
    }
  },
  "serviceSurfaces": [
    {
      "name": "docs-sdk",
      "kind": "sdk",
      "status": "supported",
      "authMode": "none",
      "exportSubpath": "."
    },
    {
      "name": "docs-cli",
      "kind": "cli",
      "status": "supported",
      "bin": "docs",
      "authMode": "local-only"
    }
  ]
}
