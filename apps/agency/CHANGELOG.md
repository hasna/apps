# @hasna/agency

## 0.3.3

### Patch Changes

- Version bump 0.3.2 → 0.3.3 (2026-08-30): the number 0.3.2 is permanently locked on the npm registry — @hasna/agency had prior published history under the scoped name (0.2.0/0.3.0/0.3.1/0.3.2, 0.3.2 published 2026-08-21T11:29Z) and the whole package was unpublished 2026-08-28T09:36:16.822Z; npm refuses to republish a previously published version number (E400 "Cannot publish over previously published version", measured 2026-08-30 at 06:55Z and 07:10Z, ~45.5h after the unpublish — npm's documented rule is that `package-name@version` is unique and cannot be reused by unpublishing and re-publishing it). This 0.3.3 carries the fully-reviewed 0.3.2 candidate code (release-gate remediation cycles 1-5; [REVIEW] GO @ b589c107d6d53cf544fd23681315d306d5fc0fca, hasna/apps#1471) plus the 0.3.3 release-review remediations: the build cleans `dist` before bundling, and the packed content is bound to the reviewed source — `dist/index.js` is committed and `prepack` rebuilds then refuses to publish unless the rebuild is byte-identical to the committed (reviewed) bundle, so a post-review mutation of source or dependencies cannot reach the tarball. Corrects the 0.3.2 changelog claim "first publication under `@hasna/agency`": the package had prior registry history under the scoped name before the 2026-08-28 unpublish; 0.3.3 is the first publication after that unpublish.

## 0.3.2

### Patch Changes

- 0a462730c (release-gate remediation): the release candidate 0.3.2 carries the 2026-08-29/30 release-gate remediation — argv-based process execution for operator- and disk-influenced values (search/export/backup/db/release/new paths and identifiers, with a strict scaffold-name grammar), a residue-free full-preimage snapshot + rollback for restore/import, fail-closed config reads with mode-preserving atomic writes for `connect`, a hard-deadline MCP timeout with nonzero exit on failures, an exact `--reviewed-sha` release gate (the command publishes the reviewed commit byte-for-byte with no post-review mutation and no direct push), remote repo creation and scaffold publication removed, JSON export aborting nonzero on any omitted table, and the exact `@types/bun` 1.3.14 pin. The 2026-08-30 cycle-2 remediation (codewith NO_GO @ 8645e0d7f) closes the remaining P1 classes: `release` publishes ONLY the exact verified tarball (`npm pack` with `--ignore-scripts`, name/version/files bound to the reviewed directory) through the vault-backed route (`secrets exec hasna/npm/live/publish-token`) with the temp npmrc outside the package tree and the vault gate failing closed BEFORE any build; a failed `git status` is a hard refusal (never a clean tree); the pre-copy snapshot lives in a mode-0700 sibling dir as a mode-0600 archive and rollback restores via an atomic swap; `agency new` creates external todos records only with the explicit `--create-tasks` flag (IDs travel as argv after strict validation) and a failed `--provision-db` terminates nonzero without the success summary; `connect` validates TOML structurally (duplicate keys and valueless keys are refused before any write). The 2026-08-30 cycle-3 remediation (codewith NO_GO @ 3f1611276) closes the last two P1 classes: `release` verifies the packed FILE SET and CONTENT of the exact tarball before any publish — every packed entry must be metadata or declared-`files` content (environment files, `node_modules`, and git metadata are refused at any depth), a missing `files` array is a hard refusal, and every packed file is scanned for internal-infra strings (`*.hasna.xyz`, ARNs, 12-digit AWS account ids) and credential-value shapes (npm/GitHub/Anthropic/OpenAI/Google/AWS/xAI prefixes), failing closed with the file and pattern class named — and the default no-argument release path now selects CLEAN reviewed candidates too (a single `--reviewed-sha` binds to the repo(s) whose HEAD equals it; repos at other heads are skipped with a note and a sha matching no repo fails closed), so the documented `agency release` workflow is reachable. The 2026-08-30 cycle-4 remediation (codewith NO_GO @ 6f2c8b8f9) closes the final two P1 classes: the packed file set and manifest now bind to a PRE-BUILD snapshot of the reviewed source manifest — the source `files` array is captured before the build, the packed manifest must equal the reviewed source manifest exactly, and a post-build `git status` that is anything but clean (a build that rewrote `package.json` or dropped payload files) fails the release closed — and the content scan now mirrors the repo publish guard's public-package prohibitions (`hasna-internal`, `internal-apps`) and covers the full GitHub token prefix family (the classic p, the o/u/s/r prefixes and the fine-grained `github_pat_` form). The 2026-08-30 cycle-5 remediation (codewith NO_GO @ a0a5cc836) restores the classic `p` prefix the o/u/s/r rewrite had dropped: a packed classic personal-access-token credential now fails the scan closed, with a regression test binding the class.
- 06ecb99: Reconstruct @hasna/agency source from the historical 0.3.1 artifact (published under the legacy unscoped name `hasna-agency`, row 91a7b09d; that name is no longer on the npm registry): the upstream repo was deleted and the tarball shipped only package.json + a bundled dist/index.js. The reconstructed tree restores a maintainable source (src/registry.ts, src/utils.ts, src/commands/*, src/db/database.ts reimplemented from the bundle's scaffold-template strings) with the documented 16-verb CLI surface: status, doctor, init, update, sync, mcp, backup, db, connect, playground, logs, search, export, import, new, release. **This 0.3.2 was intended as the first publication under `@hasna/agency` (corrected 2026-08-30: the scoped name had earlier history — 0.2.0/0.3.0/0.3.1/0.3.2, published 2026-08-21, whole package unpublished 2026-08-28 — and 0.3.2 itself is permanently locked on the registry, so it was never published; the 0.3.3 release is the first publication after the unpublish)**; the CLI surface is asserted by the in-tree behavioral parity suite (no byte-comparison against a pinned legacy artifact). Recorded deviations: (a) CLI-only member — the artifact never shipped MCP/-serve/sdk surfaces; (b) the 0.3.1 init prompt defaulted to a live internal RDS endpoint which is not carried into the public source; (c) `files` gains LICENSE, plus a `prepack` build so the publish guard packs a real dist. The embedded 45-package REGISTRY is stale by design (~28% of first-party packages) and is not the package census.
