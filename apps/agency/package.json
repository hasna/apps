{
  "name": "@hasna/agency",
  "version": "0.3.2",
  "description": "Unified management CLI for all @hasna/* open-source packages",
  "type": "module",
  "bin": {
    "agency": "dist/index.js"
  },
  "license": "Apache-2.0",
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "files": [
    "dist",
    "LICENSE"
  ],
  "dependencies": {
    "commander": "^12",
    "chalk": "^5",
    "@hasna/cloud": "^0.1.5"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^5"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "prepack": "bun run build"
  }
}
