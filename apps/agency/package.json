{
  "name": "@hasna/agency",
  "version": "0.3.4",
  "description": "Unified management CLI for all @hasna/* open-source packages",
  "type": "module",
  "bin": {
    "agency": "dist/index.js"
  },
  "license": "Apache-2.0",
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "files": [
    "dist",
    "LICENSE"
  ],
  "dependencies": {
    "@hasna/cloud": "^0.1.5",
    "chalk": "^5",
    "commander": "^12",
    "smol-toml": "^1.8.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^5"
  },
  "scripts": {
    "build": "rm -rf dist && bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "prepack": "bun run build && git diff --exit-code -- dist"
  }
}
