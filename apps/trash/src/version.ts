/** Kept in step with package.json by the CLI contract test. */
export const VERSION = "0.1.1";
export const IDENTITY = { name: "@hasna/trash", version: VERSION, guardProtocol: "hasna.trash.guard.v1" } as const;
