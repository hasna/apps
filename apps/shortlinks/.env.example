# Optional: override the local SQLite database path. Only meaningful under the
# explicit local opt-in HASNA_SHORTLINKS_LOCAL=1 (alias SHORTLINKS_LOCAL=1);
# it does not select the local backend by itself.
# SHORTLINKS_DB=/Users/you/.hasna/shortlinks/shortlinks.db

# Hosted server data backend (shortlinks-serve): PostgreSQL is required.
# Missing HASNA_SHORTLINKS_DATABASE_URL fails closed before binding a socket;
# the hosted service never falls back to the on-box SQLite client store.
# HASNA_SHORTLINKS_DATABASE_URL=postgres://shortlinks:password@db.example.com:5432/shortlinks

# Optional: salt used before hashing click IP addresses.
# SHORTLINKS_CLICK_SALT=change-me

# Hosted server-to-server Domains integration. Inject the scoped credential
# from the deployment secret manager; never commit the value. Hosted Hasna
# defaults to https://api.hasna.com/domains/v1, while self-hosters may point at
# their own Domains service root.
# HASNA_DOMAINS_API_KEY=<domains-read-and-purchase-service-key>
# HASNA_DOMAINS_API_URL=https://domains.example.net

# Optional reconciliation cadence for pending Domains jobs (milliseconds).
# SHORTLINKS_DOMAINS_RECONCILE_INTERVAL_MS=5000

# Hosted redirect plane: inject HASNA_LINK_ROUTER_SHARED_SECRET from the server secret manager.
