{
  "name": "@hasna/shortlinks-sdk",
  "version": "1.0.0",
  "description": "Zero-dependency TypeScript client for the @hasna/shortlinks REST API",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "src/index.ts",
    "src/generated.ts"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "license": "Apache-2.0",
  "scripts": {
    "build": "rm -rf dist && bun build src/index.ts --outdir dist --target node && bunx tsc src/index.ts --declaration --emitDeclarationOnly --outDir dist --target ES2022 --module ESNext --moduleResolution Bundler --skipLibCheck --lib ES2022,DOM --types bun",
    "prepare": "bun run build",
    "prepack": "bun run build"
  }
}
