{
  "schema": "hasna.service_contract.v1",
  "name": "shortlinks",
  "class": "cli-with-store",
  "contractVersion": "v1",
  "kitVersion": "1.0.2",
  "description": "Open-source shortlink manager for custom domains, click tracking, and Cloudflare/Postgres runtime support, with a CLI, MCP server, REST API (shortlinks-serve), and a generated SDK. shortlinks-serve is PostgreSQL-only and fails closed without HASNA_SHORTLINKS_DATABASE_URL. Clients resolve their authority and credential through the @hasna/contracts 1.0.2 client chain (Keychain, ~/.hasna/shortlinks/config/credentials, HASNA_SHORTLINKS_* env, default fleet gateway app base) and append /v1 exactly once, or use the on-box SQLite store only under the explicit HASNA_SHORTLINKS_LOCAL=1 opt-in. Public redirect-server helpers require an injected store or that same explicit local opt-in.",
  "bins": [
    "shortlinks",
    "shortlinks-mcp",
    "shortlinks-serve"
  ],
  "storage": {
    "backend": "postgresql",
    "engines": [
      "sqlite",
      "postgresql"
    ],
    "envPrefix": "HASNA_SHORTLINKS_",
    "aliasEnvPrefix": "SHORTLINKS_",
    "pgTestGate": {
      "envVar": "SHORTLINKS_TEST_DATABASE_URL",
      "command": "bun run test:pg"
    },
    "sqlitePath": "~/.hasna/shortlinks/shortlinks.db"
  },
  "serviceSurfaces": [
    {
      "name": "http-api",
      "kind": "api",
      "status": "supported",
      "bin": "shortlinks-serve",
      "authMode": "api-key",
      "health": {
        "method": "GET",
        "path": "/health",
        "public": true
      },
      "readiness": {
        "method": "GET",
        "path": "/ready",
        "public": true
      },
      "version": {
        "method": "GET",
        "path": "/version",
        "public": true
      },
      "apiBasePath": "/v1",
      "openApiPath": "/openapi.json"
    },
    {
      "name": "typescript-sdk",
      "kind": "sdk",
      "status": "supported",
      "authMode": "api-key",
      "exportSubpath": "./sdk",
      "clientClassName": "ShortlinksApiClient",
      "generatedFrom": "/openapi.json"
    },
    {
      "name": "mcp",
      "kind": "mcp",
      "status": "supported",
      "mcpBin": "shortlinks-mcp",
      "authMode": "api-key"
    },
    {
      "name": "cli",
      "kind": "cli",
      "status": "supported",
      "bin": "shortlinks",
      "authMode": "api-key"
    }
  ],
  "metadata": {
    "release": {
      "artifactScan": {
        "script": "scan:artifact"
      }
    },
    "serve": {
      "port": 8080,
      "probes": [
        "/health",
        "/ready",
        "/version"
      ],
      "openapi": "/openapi.json",
      "apiVersion": "v1",
      "auth": "api-key (@hasna/contracts)"
    }
  }
}
