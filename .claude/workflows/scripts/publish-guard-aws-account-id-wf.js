export const meta = {
  name: 'publish-guard-aws-account-id-r2',
  description: 'CYCLE 2 (FINAL) remediation for row 27d2a7a2 (PR 798, publish-guard aws-account-id class): cycle-1 review NO_GO with TWO in-scope P1s in apps/knowledge — (1) --external @hasna/contracts creates an undeclared runtime dependency (contracts in devDependencies only; post-fix bundles import @hasna/contracts/auth + client + client/storage which npm will not install for consumers -> module-not-found on serve/storage/auth); remediation per reviewer: declare @hasna/contracts in knowledge dependencies (matching #795 economy fix). (2) committed generated bin/ + dist/ stale at head (verify:generated rc=1 vs rc=0 at main; CI verify-generated fails for this-lane reasons once the root lockfile residual clears); remediation: bun run build under the bun 1.3.14 pin + commit regenerated artifacts in the same commit. P2: PR body suite counts inaccurate (testers 954/71 not 1025/0; browser 513/14/1 not 528/0) — correct the evidence. Lane: rebase PR 798 onto LIVE origin/main tip, apply the two P1 remediations + evidence correction in the knowledge member, re-verify (guard per member green, knowledge suite, verify:generated rc=0, frozen install, CI per-check vs live main), fresh same-lens verdict, base gate + LAND IMMEDIATELY, complete row. Two-cycle cap: FINAL cycle — on NO_GO the candidate terminates.',
  phases: [
    { title: 'Remediate', detail: 'idempotency check; rebase PR 798 onto LIVE origin/main tip; apply P1-1 (knowledge dependencies declaration) + P1-2 (regenerate + commit bin/ + dist/) + P2 evidence correction; push' },
    { title: 'Verify-2', detail: 'guard per member green; knowledge suite; verify:generated rc=0; frozen install; CI per-check vs live main; diff gate; secrets' },
    { title: 'Review-2', detail: 'same-lens Fable re-review at the final head; scope = the two P1 remediations + direct regressions only' },
    { title: 'Land', detail: 'base-movement gate vs LIVE origin/main + squash merge --body-file Agent trailer + complete row 27d2a7a2' },
  ],
}

const MONOREPO = '/home/hasna/workspace/repos/hasna/apps'
const ROW = '27d2a7a2'
const PR = '798'
const OLD_HEAD = 'f95df284aaec58d423b544f835acaaf728d1d875'

const CONST = `
You are the publish-guard-aws-account-id CYCLE-2 (FINAL) remediation lane (row ${ROW}; owner-authorized via the task-drain queue; cycle 2 of 2 in the SAME review lineage — the bounded-review cap means NO further cycle exists after this one). Final text = machine-readable JSON.

Context: the cycle-1 lane (wf_3541a53a-20a) delivered PR ${PR} at head ${OLD_HEAD} fixing the aws-account-id guard violations for 7 members (browser, holdings, testers, knowledge, consolidations, prompts, servers — economy fixed separately by publish-all's release lane) — guard per member 0 violations at the head, red-before measured at main. The cycle-1 review NO_GO returned TWO in-scope P1s, BOTH in apps/knowledge:
- P1-1: the fix adds '--external @hasna/contracts' to the four knowledge build invocations, but knowledge's package.json declares @hasna/contracts ONLY in devDependencies — post-fix bundles (bin/knowledge-serve.js, bin/knowledge.js, bin/knowledge-mcp.js, dist/index.js, dist/serve.js) import from '@hasna/contracts/auth', '@hasna/contracts/client', '@hasna/contracts/client/storage' which npm will not install for consumers -> module-not-found at runtime on serve/storage/auth paths. REMEDIATION (reviewer-named, in-scope): declare @hasna/contracts in knowledge's dependencies (the version the source imports, matching how PR 795 fixed economy).
- P1-2: committed generated artifacts stale at the head — 'bun run verify:generated' in apps/knowledge returns rc=1 at head vs rc=0 at CURRENT origin/main; CI verify-generated fails for this-lane reasons once the root lockfile drift (d175d558 lineage) clears. REMEDIATION: 'bun run build' in apps/knowledge under the load-bearing bun 1.3.14 pin and commit the regenerated bin/ + dist/ in the SAME commit as P1-1.
P2 (evidence-quality, non-blocking but to correct): the PR body claimed testers 1025/0 and browser 528/0; measured at the head: testers 954 pass/71 fail, browser 513 pass/14 skip/1 fail (failing sets reproduce identically at CURRENT origin/main for testers/holdings; browser fails FEWER at head than at main). Correct the PR body's suite counts to the measured values.

Non-negotiable rules:
- IDEMPOTENCY CHECK FIRST: (a) row ${ROW} is pending and unowned; (b) PR ${PR} is OPEN with headRefOid exactly ${OLD_HEAD}; (c) no OTHER open PR fixes this class; (d) the cycle-1 NO_GO + resume condition are on record (row + PR comments). If ANY check fails, record the exact state and STOP.
- ${MONOREPO} is READ/context only. Sync first (git -C ${MONOREPO} fetch origin main -q; never discard local work). Resolve CURRENT origin/main from FETCH_HEAD and verify FETCH_HEAD == gh api repos/hasna/apps/commits/heads/main --jq .sha. MAIN MOVES — re-measure the tip at the start, after every rebase, and immediately before the land gate; whenever the tip advanced, re-rebase onto the NEW tip. The final head MUST sit directly on the live tip measured at land time.
- REMEDIATE (in the lane worktree ~/.hasna/repos/worktrees/apps/publish-guard-aws-account-id-r2 cut from the LIVE origin/main tip): rebase fix/publish-guard-aws-account-id onto the live tip; then apply ONLY: (1) knowledge package.json — move/add @hasna/contracts (the version the source imports, currently 0.13.3) to dependencies; (2) regenerate apps/knowledge bin/ + dist/ with 'bun run build' (bun 1.3.14 pin) and commit the regenerated artifacts; (3) correct the PR body suite counts to the measured values (testers 954/71, browser 513/14/1 at the head). Do NOT change anything else. Push the rebased head with force-with-lease ONLY after verifying the remote head still equals ${OLD_HEAD}.
- VERIFY at the final head (bounded): pack + guard-scan per member green (literal per-member lines: browser, holdings, testers, knowledge, consolidations, prompts, servers — 0 violations each); 'bun run verify:generated' in apps/knowledge rc=0 (literal); knowledge suite green (literal counts — classify every failure vs CURRENT origin/main: identical failing-name sets = main-state residual); 'bun install --frozen-lockfile' rc=0 (bun 1.3.14, zero node_modules, literal); CI per-check table at the final head (bounded polling — classify EVERY failure against the LIVE origin/main state: root lockfile drift (d175d558), recordings/todos pack, contracts 0.13.3 standard-adherence, versioning-integrity must fail identically on main; this-lane-caused failures MUST be green); diff gate (the 7 members + changeset only); secrets scan clean.
- REVIEW-2 (one Fable adversarial reviewer, SAME lens — publish-guard-aws-account-id-review): re-review ONLY the two P1 remediations and their direct regressions: (1) @hasna/contracts declared in knowledge dependencies so the packed tarball's bundles resolve for clean consumers (literal: package.json dependencies contains @hasna/contracts; fresh-consumer resolution proof where feasible); (2) verify:generated rc=0 at the final head (literal) and committed artifacts byte-match a rebuild; (3) guard per member 0 violations at the final head (literal); (4) knowledge suite green with named main-state residuals; (5) base-movement gate vs the LIVE origin/main — merge-tree == head tree (literal); (6) no other-lane interference; (7) the PR body evidence correction is present. Do NOT relitigate the GO'd fix shape and do NOT discover new issues outside the remediations. Post '[REVIEW] <GO|NO_GO> — publish-guard-aws-account-id-r2 @ <sha> — lens: cycle-1 P1 remediations closed, reviewer publish-guard-aws-account-id-review' to #board. Block ONLY concrete P0/P1 defects.
- LAND: on GO, re-measure the LIVE origin/main tip ONE MORE TIME, base-movement gate (merge-tree vs THAT tip; <merge-ref>^{tree} == <head>^{tree}), gh pr merge ${PR} --squash --body-file ending 'Agent: publish-guard-aws-account-id-r2-land' (last line; never Co-Authored-By), record the merged sha, LIVE-VERIFY the pack + guard for the touched members at the merged main tip (bounded), complete row ${ROW} with evidence. LAND IMMEDIATELY — do not defer. On NO_GO: comment findings + resume condition, leave open, row stays pending (candidate terminates — no further cycle exists).
- No secrets: never print/capture/commit credential values (the account id IS an internal-infra string — do not print or commit it; reference it only as 'the account id'); no internal-infra strings in any output beyond the violation listing itself. Capture path: redirect to files, read both + $?, never pipe large reads. Paste literal output lines. Record as you go: comments on PR ${PR} and row ${ROW}, posts to #board. English. Distinguish measured vs inferred; state what you did not check. The apps project is 3bbc22e0-205f-4e3d-8c5a-d8ce8e99afd8.
`

const REMEDIATE = CONST + `
ROLE: remediate lane (Opus). IDEMPOTENCY CHECK FIRST (per CONST). Rebase PR ${PR} onto the LIVE origin/main tip in the lane worktree; apply ONLY the two P1 remediations (knowledge dependencies declaration; regenerate + commit apps/knowledge bin/ + dist/ under bun 1.3.14) + the P2 evidence correction (PR body suite counts); re-rebase onto every newer tip; push (force-with-lease after verifying remote head == ${OLD_HEAD}); run the gates at the final head (guard per member, knowledge verify:generated, knowledge suite with main-state classification, frozen install, CI per-check vs the LIVE tip, diff gate, secrets). Return (JSON): { mainTip, newHead, diffFiles: [string], guardPerMember: [{member, rc, violations}], verifyGeneratedRc, suiteCounts: {passed, failed}, frozenInstallRc, ciGreen, checks: [{name, conclusion, classification}], diffGatePass, secretsClean, pushed, evidence }
`

const REVIEW2 = CONST + `
ROLE: cycle-2 re-reviewer (Fable, SAME lens — publish-guard-aws-account-id-review). Re-review ONLY the two P1 remediations and their direct regressions (per CONST): (1) @hasna/contracts declared in knowledge dependencies (fresh-consumer resolution proof where feasible), (2) verify:generated rc=0 at the final head (literal) with committed artifacts byte-matching a rebuild, (3) guard per member 0 violations (literal), (4) knowledge suite green with named main-state residuals, (5) base-movement gate vs the LIVE origin/main (merge-tree == head tree), (6) no other-lane interference, (7) PR body evidence correction present. Do NOT relitigate the GO'd fix shape or discover unrelated issues. Post '[REVIEW] <GO|NO_GO> — publish-guard-aws-account-id-r2 @ <sha> — lens: cycle-1 P1 remediations closed, reviewer publish-guard-aws-account-id-review' to #board. Return (JSON): { verdict: 'GO'|'NO_GO', findings: [{severity, title, detail}] }
`

const LAND = CONST + `
ROLE: land lane. If GO: re-measure the LIVE origin/main tip, base-movement gate (merge-tree vs that tip; <merge-ref>^{tree} == <head>^{tree}), gh pr merge ${PR} --squash --body-file ending 'Agent: publish-guard-aws-account-id-r2-land', record merged sha, LIVE-VERIFY pack + guard for the touched members at the merged main tip (bounded), complete row ${ROW} with evidence. LAND IMMEDIATELY. If NO_GO: comment findings + resume condition, leave open. Return (JSON): { merged, mergedSha, liveGuardMembers: [{member, rc}], rowState, residue: [] }
`

const REMEDIATE_SCHEMA = { type: 'object', properties: { mainTip: { type: 'string' }, newHead: { type: 'string' }, diffFiles: { type: 'array' }, guardPerMember: { type: 'array' }, verifyGeneratedRc: { type: 'number' }, suiteCounts: { type: 'object' }, frozenInstallRc: { type: 'number' }, ciGreen: { type: 'boolean' }, checks: { type: 'array' }, diffGatePass: { type: 'boolean' }, secretsClean: { type: 'boolean' }, pushed: { type: 'boolean' }, evidence: { type: 'string' } }, required: ['newHead', 'pushed', 'guardPerMember', 'verifyGeneratedRc'] }
const REVIEW2_SCHEMA = { type: 'object', properties: { verdict: { type: 'string' }, findings: { type: 'array' } }, required: ['verdict'] }
const LAND_SCHEMA = { type: 'object', properties: { merged: { type: 'boolean' }, mergedSha: { type: ['string', 'null'] }, liveGuardMembers: { type: 'array' }, rowState: { type: 'string' }, residue: { type: 'array' } }, required: ['merged'] }

phase('Remediate')
const remediate = await agent(REMEDIATE, { label: 'awsc-r2-remediate', phase: 'Remediate', schema: REMEDIATE_SCHEMA, model: 'opus' })

phase('Review-2')
const review2 = remediate && remediate.pushed && remediate.verifyGeneratedRc === 0
  ? await agent(REVIEW2, { label: 'awsc-r2-review2', phase: 'Review-2', schema: REVIEW2_SCHEMA, model: 'fable' })
  : { verdict: 'NO_GO', findings: [{ severity: 'P1', title: 'remediation did not complete (verify:generated must be rc=0)', detail: JSON.stringify({ remediate }) }] }

phase('Land')
const land = review2 && review2.verdict === 'GO'
  ? await agent(LAND, { label: 'awsc-r2-land', phase: 'Land', schema: LAND_SCHEMA })
  : { merged: false, mergedSha: null, liveGuardMembers: [], rowState: 'pending', residue: ['NO_GO at cycle 2 — candidate terminated; adjudication required'] }

return { remediate: remediate && { newHead: remediate.newHead, verifyGeneratedRc: remediate.verifyGeneratedRc, guardPerMember: remediate.guardPerMember }, review2: review2 && review2.verdict, land }
